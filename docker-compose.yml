services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: mmrag-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"   # REST API + web dashboard
      - "6334:6334"   # gRPC (faster client calls)
    volumes:
      - ./qdrant_storage:/qdrant/storage
